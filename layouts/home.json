[
{{- $posts := where site.RegularPages "Section" "posts" -}}
{{- range $index, $post := $posts -}}
  {{- if $index }},{{ end }}
  {
    "title": {{ $post.Title | jsonify }},
    "url": {{ $post.RelPermalink | jsonify }},
    "date": {{ $post.Date.Format "2006-01-02" | jsonify }},
    "description": {{ ($post.Description | default ($post.Plain | plainify | truncate 240)) | jsonify }},
    "content": {{ ($post.Plain | plainify) | jsonify }},
    "tags": {{ ($post.Params.tags | default (slice)) | jsonify }}
  }
{{- end -}}
]
